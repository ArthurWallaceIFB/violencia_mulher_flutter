import 'package:flutter/material.dart';

class PedidosView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pedidos'),
      ),
      body: Center(
        child: Text('Conteúdo dos Pedidos'),
      ),
    );
  }
}
